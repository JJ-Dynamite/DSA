const binarySearch = (arr, target) => {
	return helper(arr, target, 0, arr.length - 1)
}
let helper=(arr,target,first,last)=>{
    if(first>last){}
}

// if (binarySearch([1, 3, 5, 7, 8, 9],5))

//     console.log("Element found!")
// else
//     console.log("Element not found!")